interface:
  display_name: "Task Planner"
  short_description: "Break complex work into executable plans."
  default_prompt: "Use $task-planner to create an ordered execution plan with validation and risk controls."
policy:
  allow_implicit_invocation: true