interface:
  display_name: "Core Context Router"
  short_description: "Route tasks to minimal instruction context."
  default_prompt: "Use $core-context-router to select the minimal context pack for this request."
policy:
  allow_implicit_invocation: true