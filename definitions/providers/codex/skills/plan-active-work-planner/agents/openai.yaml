interface:
  display_name: "Active Work Planner"
  short_description: "Create or update versioned active plans under planning/."
  default_prompt: "Use $plan-active-work-planner to create or update the active execution plan with validations, specialist routing, and closeout requirements."
policy:
  allow_implicit_invocation: true