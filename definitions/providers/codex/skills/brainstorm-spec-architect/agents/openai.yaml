interface:
  display_name: "Brainstorm Spec Architect"
  short_description: "Create or update the repository-owned spec before execution planning for non-trivial work."
  default_prompt: "Use $brainstorm-spec-architect after intake to decide whether a versioned spec is required, create or update it under planning/specs/active, and prepare the workstream for deterministic planning."
policy:
  allow_implicit_invocation: true