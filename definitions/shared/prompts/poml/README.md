# Shared POML Library

This folder is the authoritative source for the reusable POML prompt library.

Projected runtime/editor surface:

- `.github/prompts/poml/`

Provider-specific prompt entrypoints remain under
`definitions/providers/github/prompts/*.prompt.md`.